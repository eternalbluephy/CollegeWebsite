CREATE TABLE `college_website`.`content` (
                                             `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
                                             `content` LONGTEXT NOT NULL,
                                             PRIMARY KEY (`id`));
